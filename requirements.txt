requests>=2.31
python-dotenv>=1.0
pandas>=2.0
# pyarrow 25.0.0 segfaultea en Linux (bug de mimalloc al cargar libarrow fuera del
# hilo principal) — tumba la app en Streamlit Cloud al interactuar con dataframes.
# streamlit>=1.59.2 trae el workaround; el pin de pyarrow es el cinturón extra.
pyarrow>=15.0,!=25.0.0
streamlit>=1.59.2
plotly>=5.20
